name: .NET Desktop

on:
  workflow_call:
  push:
    branches: [main]
    paths:
      - "WondayWall/**"
      - "WondayWall.Package/**"
      - "WondayWall.Wix/**"
      - "WondayWall.sln"
      - "GitVersion.yml"
      - ".config/**"
      - "eng/**"
      - ".github/workflows/dotnet-desktop.yml"
  pull_request:
    branches: [main]
    paths:
      - "WondayWall/**"
      - "WondayWall.Package/**"
      - "WondayWall.Wix/**"
      - "WondayWall.sln"
      - "GitVersion.yml"
      - ".config/**"
      - "eng/**"
      - ".github/workflows/dotnet-desktop.yml"

env:
  NUGET_PACKAGES: ${{ github.workspace }}\.nuget\packages

jobs:
  license:
    runs-on: windows-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7.0.0

      - uses: actions/setup-dotnet@v5.4.0
        with:
          dotnet-version: 10.0.x

      - uses: actions/cache@v6
        with:
          path: ${{ env.NUGET_PACKAGES }}
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/packages.lock.json', '**/*.csproj', '**/*.wixproj', '.config/dotnet-tools.json', 'eng/licenses/*.json', 'Directory.Packages.props', 'Directory.Build.props', 'NuGet.config', 'global.json') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore dotnet tools
        run: dotnet tool restore

      - name: Restore NuGet packages
        run: dotnet restore WondayWall.sln

      - name: Generate third-party notices
        run: |
          New-Item -ItemType Directory -Force licenses
          dotnet tool run nuget-license `
            -i WondayWall.sln `
            -t `
            -a eng\licenses\allowed-licenses.json `
            -override eng\licenses\package-overrides.json `
            -ignore eng\licenses\license-download-ignored-packages.json `
            -d licenses `
            -fo licenses\THIRD-PARTY-NOTICES.md `
            -o Markdown `
            -err

      - name: Verify THIRD-PARTY-NOTICES.md is not empty
        run: |
          if (!(Test-Path "licenses\THIRD-PARTY-NOTICES.md")) {
            throw "THIRD-PARTY-NOTICES.md was not found."
          }
          if ((Get-Item "licenses\THIRD-PARTY-NOTICES.md").Length -eq 0) {
            throw "THIRD-PARTY-NOTICES.md is empty."
          }

      - uses: actions/upload-artifact@v7
        with:
          name: licenses
          path: licenses\

  build:
    needs:
      - license
    strategy:
      matrix:
        self: [true, false]
    runs-on: windows-latest
    permissions:
      contents: read
    outputs:
      version: ${{ steps.gitversion.outputs.assemblySemFileVer }}
      msix_version: ${{ steps.build_number.outputs.version }}
    steps:
      - uses: actions/checkout@v7.0.0
        with:
          fetch-depth: 0
      - uses: actions/cache@v6
        with:
          path: ${{ env.NUGET_PACKAGES }}
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/packages.lock.json', '**/*.csproj', '**/*.wixproj', '.config/dotnet-tools.json', 'eng/licenses/*.json', 'Directory.Packages.props', 'Directory.Build.props', 'NuGet.config', 'global.json') }}
          restore-keys: |
            ${{ runner.os }}-nuget-
      - uses: actions/setup-dotnet@v5.4.0
        with:
          dotnet-version: 10.0.x
      - uses: gittools/actions/gitversion/setup@v4.5.0
        with:
          versionSpec: "6.x"
      - id: gitversion
        uses: gittools/actions/gitversion/execute@v4.5.0
      - id: build_number
        name: Determine MSIX build number
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $versionComponents = '${{ steps.gitversion.outputs.assemblySemFileVer }}'.Split('.')
          if ($versionComponents.Count -ne 4) {
            throw "GitVersion did not produce a four-part file version."
          }

          $buildNumber = [int](git rev-list --count HEAD)
          if ($buildNumber -gt 65535) {
            throw "The MSIX build number $buildNumber exceeds the maximum value 65535."
          }

          $versionComponents[3] = $buildNumber.ToString()
          $msixVersion = $versionComponents -join '.'
          "version=$msixVersion" >> $env:GITHUB_OUTPUT
          Write-Host "MSIX version: $msixVersion"
      - uses: actions/download-artifact@v8
        with:
          name: licenses
          path: publish\licenses
      - run: |
          dotnet publish WondayWall -c Release -o publish `
            -p:DebugType=None `
            -p:RuntimeIdentifier=win-x64 `
            -p:PublishSingleFile=true `
            -p:PublishSelfContained=${{ matrix.self }} `
            -p:Version=${{ steps.gitversion.outputs.fullSemVer }} `
            -p:AssemblyVersion=${{ steps.gitversion.outputs.assemblySemVer }} `
            -p:FileVersion=${{ steps.gitversion.outputs.assemblySemFileVer }} `
            -p:InformationalVersion=${{ steps.gitversion.outputs.informationalVersion }}
      - name: Verify THIRD-PARTY-NOTICES.md in publish output
        run: |
          if (!(Test-Path "publish\licenses\THIRD-PARTY-NOTICES.md")) {
            throw "THIRD-PARTY-NOTICES.md was not found in publish output."
          }
      - uses: actions/upload-artifact@v7
        with:
          name: WondayWall${{ matrix.self && '-full' || '' }}-${{ steps.gitversion.outputs.fullSemVer }}
          path: publish\

  installer:
    needs:
      - build
    runs-on: windows-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7.0.0

      - uses: actions/cache@v6
        with:
          path: ${{ env.NUGET_PACKAGES }}
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/packages.lock.json', '**/*.csproj', '**/*.wixproj', '.config/dotnet-tools.json', 'eng/licenses/*.json', 'Directory.Packages.props', 'Directory.Build.props', 'NuGet.config', 'global.json') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - uses: actions/setup-dotnet@v5.4.0
        with:
          dotnet-version: 10.0.x

      # WondayWall-full-* は SelfContained=true のビルド成果物
      - uses: actions/download-artifact@v8
        with:
          path: artifacts
          pattern: WondayWall-full-*
          merge-multiple: true

      - name: Verify THIRD-PARTY-NOTICES.md in artifacts
        run: |
          if (!(Test-Path "artifacts\licenses\THIRD-PARTY-NOTICES.md")) {
            throw "THIRD-PARTY-NOTICES.md was not found in artifacts."
          }

      - run: |
          dotnet run --project WondayWall.Wix.csproj -c Release
          dotnet build WondayWall.Installer.wixproj -p:OutputName=WondayWall-${{ needs.build.outputs.version }}
        working-directory: WondayWall.Wix

      - name: Generate MSIX
        shell: pwsh
        run: |
          $version = '${{ needs.build.outputs.msix_version }}'

          # Windows SDK に同梱の makeappx.exe を検索
          $makeappx = Get-ChildItem 'C:\Program Files (x86)\Windows Kits\10\bin' -Filter 'makeappx.exe' -Recurse -ErrorAction SilentlyContinue |
            Where-Object { $_.FullName -match 'x64' } |
            Sort-Object FullName -Descending |
            Select-Object -First 1 -ExpandProperty FullName

          if (-not $makeappx) {
            throw 'makeappx.exe が見つかりません。Windows SDK が必要です。'
          }

          Write-Host "makeappx.exe: $makeappx"

          # MSIX 用アイコン画像を各ロゴサイズへリサイズして生成
          $iconSrc = 'docs\assets\icon_512.png'
          $imagesDir = 'msix-staging\Images'
          New-Item -ItemType Directory -Force -Path $imagesDir | Out-Null
          magick $iconSrc -resize 44x44 "$imagesDir\Square44x44Logo.png"
          magick $iconSrc -resize 150x150 "$imagesDir\Square150x150Logo.png"
          magick $iconSrc -resize 310x150 -background transparent -gravity center -extent 310x150 "$imagesDir\Wide310x150Logo.png"
          magick $iconSrc -resize 50x50 "$imagesDir\StoreLogo.png"

          # アプリバイナリをステージングへコピー
          Copy-Item 'artifacts\*' 'msix-staging\' -Recurse -Force

          # マニフェストのバージョン文字列を差し替えてステージングへ配置
          $manifest = Get-Content 'WondayWall.Package\AppxManifest.xml' -Raw -Encoding UTF8
          $manifest = $manifest.Replace('Version="0.0.0.0"', "Version=`"$version`"")
          Set-Content 'msix-staging\AppxManifest.xml' $manifest -Encoding UTF8

          # MSIX をパック（/nv = 署名検証スキップ）
          New-Item -ItemType Directory -Force -Path publish | Out-Null
          & $makeappx pack /d msix-staging /p "publish\WondayWall-$version.msix" /nv
          if ($LASTEXITCODE -ne 0) {
            throw "makeappx failed with exit code $LASTEXITCODE"
          }

          Write-Host "MSIX generated: publish\WondayWall-$version.msix"

      - uses: actions/upload-artifact@v7
        with:
          name: WondayWall-${{ needs.build.outputs.version }}.msi
          path: publish\*.msi

      - uses: actions/upload-artifact@v7
        with:
          name: WondayWall-${{ needs.build.outputs.msix_version }}.msix
          path: publish\*.msix

  store-upload:
    name: Upload MSIX to main-build package flight
    if: ${{ github.event_name == 'push' && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/')) }}
    needs:
      - installer
    runs-on: windows-latest
    permissions:
      contents: read
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: WondayWall-*.msix
          path: msix-artifacts
          merge-multiple: true

      - uses: microsoft/microsoft-store-apppublisher@v1

      - name: Configure Partner Center authentication
        run: |
          msstore reconfigure `
            --tenantId "${{ secrets.AZURE_AD_TENANT_ID }}" `
            --clientId "${{ secrets.AZURE_AD_APPLICATION_CLIENT_ID }}" `
            --clientSecret "${{ secrets.AZURE_AD_APPLICATION_SECRET }}" `
            --sellerId "${{ secrets.SELLER_ID }}"

      - name: Replace pending submission and upload MSIX to main-build package flight
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $productId = "${{ vars.MSSTORE_PRODUCT_ID }}"
          $flightId = "${{ vars.MSSTORE_FLIGHT_ID }}"

          function Get-PackageFlight {
            $output = & msstore flights get $productId $flightId | Out-String
            if ($LASTEXITCODE -ne 0) {
              throw "Could not retrieve package flight '$flightId'."
            }

            try {
              return $output | ConvertFrom-Json
            }
            catch {
              throw "Could not parse package flight '$flightId'."
            }
          }

          $msixPath = Get-ChildItem -Path msix-artifacts -Filter "*.msix" |
            Select-Object -First 1 -ExpandProperty FullName
          if (-not $msixPath) {
            throw "No MSIX file found in msix-artifacts. Ensure the installer job produced a WondayWall-*.msix artifact."
          }

          $flight = Get-PackageFlight
          if ($null -ne $flight.pendingFlightSubmission) {
            if ($null -eq $flight.lastPublishedFlightSubmission) {
              throw "The first submission is still pending. Publish it once before enabling automatic replacement."
            }

            Write-Host "Canceling pending submission '$($flight.pendingFlightSubmission.id)'..."
            & msstore flights submission delete $productId $flightId --no-confirm
            if ($LASTEXITCODE -ne 0) {
              Write-Warning "The pending submission could not be canceled. Waiting for it to finish instead."
              & msstore flights submission poll $productId $flightId
              if ($LASTEXITCODE -ne 0) {
                throw "The pending submission could not be canceled or completed successfully."
              }
            }

            for ($attempt = 1; $attempt -le 12; $attempt++) {
              $flight = Get-PackageFlight
              if ($null -eq $flight.pendingFlightSubmission) {
                break
              }
              if ($attempt -eq 12) {
                throw "Pending submission was not removed within 120 seconds."
              }
              Write-Host "Waiting for pending submission removal ($attempt/12)..."
              Start-Sleep -Seconds 10
            }
          }

          for ($attempt = 1; $attempt -le 5; $attempt++) {
            & msstore publish `
              "$msixPath" `
              -id $productId `
              -f $flightId
            $publishExitCode = $LASTEXITCODE

            if ($publishExitCode -eq 0) {
              break
            }

            if ($attempt -eq 5) {
              throw "Publishing the package flight submission failed after 5 attempts."
            }

            $flight = Get-PackageFlight
            if ($null -ne $flight.pendingFlightSubmission) {
              throw "Publishing failed after creating pending submission '$($flight.pendingFlightSubmission.id)'. It was preserved for the next run."
            }

            $retryDelay = 15 * $attempt
            Write-Warning "Submission creation failed. Retrying in $retryDelay seconds ($attempt/5)..."
            Start-Sleep -Seconds $retryDelay
          }
